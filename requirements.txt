# 第一批(后端数据模型 + 规则引擎)所需依赖
sqlalchemy>=2.0
pyyaml>=6.0
pytest>=9.1.1
httpx>=0.27            # 第二批: OSV.dev 漏洞查询客户端

# 第三批(API 与前端联调):
fastapi>=0.110           # API 路由层
uvicorn>=0.29            # ASGI 服务器
python-docx>=1.1    # 「下载 Word 文档」产物导出
openpyxl>=3.1            # Excel 需求跟踪表
python-multipart>=0.0.9  # SBOM 文件上传解析
